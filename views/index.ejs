<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Here is Quora Posts</h1>
    <a href="http://localhost:8080/post/new">Add a new post</a>

    <% for(post of posts) {%>
        <div class="post">
            <h3> @<%= post.username %> </h3>
            <h3> <%= post.content %> </h3>
            <a href="http://localhost:8080/post/<%= post.id %>">View Details</a>
            <a href="http://localhost:8080/post/<%= post.id %>/edit">Edit Post</a>
            <form action="http://localhost:8080/post/<%= post.id %>?_method=DELETE" method="POST">
                <button type="submit">Delete Post</button>
            </form>
        </div>

        <% } %>


</body>
</html>